build and install new glibc
===========================
update the file: sysdeps/unix/sysv/linux/i386/syscall.S
mkdir glibc-build
cd glibc-build
export CFLAGS="-g -O2 -march=i686"
../glibc-2.9/configure --prefix=/home/jun/glibc_install
(ubuntu: CFLAGS="-O2 -U_FORTIFY_SOURCE -fno-stack-protector" ../eglibc-2.11.1/configure --prefix=/opt)
make
make install
export LD_LIBRARY_PATH=/home/jun/glibc_install/lib

/usr/local/lib/libdyninstAPI_RT.so

build new kernel
================
make distclean
make menuconfig
make
make modules_install
make install
